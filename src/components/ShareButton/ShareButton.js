import React  from 'react';

import MainButton from "../MainButton/MainButton";

const ShareButton = () => {

  return (
    <MainButton onClick={() => {}}>Поделиться</MainButton>
  )
};

export default ShareButton;
